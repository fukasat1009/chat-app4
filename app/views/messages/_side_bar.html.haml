.chat-side
  .side-header
    .side-header__name
      くま
      .side-header__name__icons
        = icon('far', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')

  .groups
    .groups__name
      sss
    .groups__message
      おはようございます


